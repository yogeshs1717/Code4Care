fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0
httpx>=0.27.0
openai>=1.50.0
google-genai>=1.0.0

# ── Personalization / auth ────────────────────────────────────────────
# Async database. aiosqlite powers local dev; asyncpg is used in production
# (e.g. Render with a PostgreSQL external database).
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
asyncpg>=0.29.0
# JWT access tokens (PyJWT) + email address validation.
PyJWT>=2.9.0
email-validator>=2.2.0
# Email OTP delivery. Resend is the MVP provider; the service falls back to
# a console-pretender when RESEND_API_KEY is unset.
resend>=0.8.0
